pipeline:
    build:
        image: debian:jessie
        commands:
            - ls -l
            - sleep 10
            - cat all.log
            - echo 'Build finished :D'
            - echo 'yep'
    other:
        image: debian:jessie
        commands:
            - ls -l
            - echo 'end of other'
